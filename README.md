# 灯川 Rillight

独立桌面影视客户端。在 Windows、macOS 与 Linux 上连接已有 Emby 服务器，浏览并播放电影与剧集。默认界面为简体中文。

本仓库只包含桌面目标（`windows` / `macos` / `linux`），不含 Android 或 iOS。

## 开发

需要已启用桌面支持的 Flutter SDK。

```sh
flutter pub get
flutter run -d windows
flutter test
```
